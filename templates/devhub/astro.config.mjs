import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://example.com',
  // Uncomment and set base path for subdirectory deployments
  // base: '/devhub',
});
